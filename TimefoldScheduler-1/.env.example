# Server port for API and client
PORT=5000

# Timefold solver API base URL (quickstart runs at 8080)
SOLVER_API_URL=http://127.0.0.1:8080

# Optional auth token if your solver requires it
SOLVER_API_TOKEN=

# Optional: mock solver port (used by `npm run solver:mock`)
SOLVER_MOCK_PORT=8081

# Database connection (required if you switch storage to Postgres or run drizzle)
# Example Neon/Cloud Postgres URI (replace with your actual credentials)
# DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require
DATABASE_URL=

# Node environment (dev/prod). In dev, Vite is enabled by the dev script
NODE_ENV=development

# Used by scripts (optional)
API_BASE=http://127.0.0.1:5000
